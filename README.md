# Purpeon Digital

IT consulting website built with [Astro](https://astro.build) and [Vue.js](https://vuejs.org).

## 🚀 Quick Start

```bash
bun install
bun dev
```

Open [http://localhost:4321](http://localhost:4321)

## 📁 Project Structure

```
src/
├── components/     # Vue components
├── layouts/        # Astro layouts
├── pages/          # Astro pages (routes)
└── styles/         # Global CSS
public/             # Static assets
```

## 🛠 Commands

| Command | Action |
|---------|--------|
| `bun dev` | Start dev server |
| `bun build` | Build for production |
| `bun preview` | Preview build |

## 🌐 Deployment

Builds to `./dist` — deploy to any static host. CNAME configured for `purpeon.no`.
